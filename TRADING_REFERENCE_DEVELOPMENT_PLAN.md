# Trading Reference Development Plan

## 1. 目标

把 FIRE Finance Workbench 从“市场研究与纸上记录工具”推进为一套可审计的交易参考系统。

系统应帮助用户回答：

1. 当前是否存在值得研究的交易机会？
2. 哪些证据支持或反驳该方向？
3. 如果参与，账户最多承担多少风险？
4. 策略在未见样本、费用和真实成交摩擦后是否仍有正期望？
5. 当前证据只允许观察、Paper、Testnet，还是可以申请进入受限实盘评审？

本计划不授权真实资金交易。主网执行必须经过单独的产品、安全和合规评审。

## 实施状态

2026-08-22 续优化：Testnet 校准与四项安全演练已接入受权限保护的自动交易面板，
校准结果会实时回传证据闸门；证据等级改为严格的顺序状态机，不能绕过样本外回测或
Paper 门槛直接升级到 Testnet。回测证据支持 JSON 导入并以原始输入重算验真，验证后
自动建立上海时区当前周/月 Paper 对比窗口；已关闭的 Paper 交易和 Testnet 校准会动态
回传证据闸门。账户、组合风险与资格控制改为15项带证据和确认时间的人工核验清单，
30天后自动失效并支持审计JSON导出；人工声明不会被标记为自动验证，并可与可重算回测、
Paper偏差、Testnet校准和最终闸门结论合并导出为综合评审包；导入
校验会检查 SHA-256 内容摘要、重跑回测、重算人工核验及重新生成闸门结论，页面提供只读验证
入口且不会覆盖当前证据。工作台会按用户实际打开的监控会话自动记录预期K线周期、
去重后的实际观测和行情中断，
页面关闭时间不会被误报为漏跑；这些原始输入随Paper偏差一并进入综合评审包并可重新计算。
崩溃恢复时，未正常结束的会话和行情缺口会封口在最后心跳时间，避免把离线时间算入断层。
遥测证据按当前周/月评审窗口裁剪，历史会话不会污染本期Paper偏差。系统支持使用
`paper.manage` 权限按用户同步回测、Paper遥测和人工核验；采用乐观修订号避免
跨设备静默覆盖，并保留每次同步的SHA-256审计记录。服务端限制资料包为2MB，重新运行
回测并拒绝疑似API Secret、私钥、云密钥或助记词内容；Testnet凭据不进入资料包。
云端保留最近20个完整证据版本，可先比较本地与选中版本的回测、遥测和核验差异；恢复历史
版本会生成新的修订号，不会覆盖或改写原历史。当前版本加载、历史预览和历史恢复都会重新
计算存储正文的 SHA-256 并核对保存摘要，损坏或非预期修改的记录不会进入评审链路。
当前版本、同步审计和历史版本改为单个 D1 事务批次写入，任一步失败都会整体回滚；每次写入
带独立同步令牌，使相同正文的并发旧请求也能稳定返回修订冲突。空库只接受修订号0，数据库
故障不会再伪装成客户端输入错误。同步审计新增逐用户 SHA-256 前序链，验证器会检查固定摘要、
修订连续性、前序关系以及当前证据链头；删除、篡改或缺失记录会显示完整性异常，迁移前的无哈希
记录只标记为部分可验证。云端面板展示验证数量、旧记录数、链头和具体问题。完整性异常或
审计状态缺失时，服务端拒绝继续扩展证据链，页面同时禁用上传、采用和历史恢复，
只保留刷新与只读差异预览。用户可导出当前链头为外部检查点；以后导入时，服务端会验证文件
内容摘要、所属用户、对应修订记录以及它是否仍是当前完整链的链头或历史祖先。检查点明确不是
数字签名，也不提供实盘授权。综合评审包 v2 可附带用户实际导入且经服务端核验有效的
外部检查点、核验时间和非签名范围声明；导入会重算检查点、锚点关系、评审包摘要和原有全部
证据。导入带锚点的评审包时会再次请求服务端核对当前用户审计链，并明确区分“云端已复验”、
“仅包内一致”和“验证失败”；网络或权限不可用时不会把离线摘要冒充当前云端复验。旧 v1
评审包继续支持导入。当前导出升级为 v3：Worker 额外提供最多500条原始 Testnet 执行观测和
100条安全演练，评审包导入会重新运行成交校准；同步修改聚合指标、闸门结论和外层摘要仍会被
原始输入重算拒绝。旧 v1/v2 的 Testnet 裸汇总可以查看，但明确标记为不可重算的旧证据，不能
显示为当前可信。人工核验 schema v1 的有效期固定为30天，导入文件不能自行延长；导入还会
按当前时点重算包龄和人工核验，过期、当前结论变化或生成时间超出5分钟时钟容差时只显示陈旧，
不会显示为当前有效。Paper 周/月窗口使用同一个上海时区边界生成和复验；即使包龄只有几分钟，
跨周或跨月后也会立即标记旧周期证据。Testnet 原始输入还会拒绝负延迟、未知命令/状态、负成交价、
非有限数量、负手续费以及非法演练类型、时间、结果或证据。Testnet 观测和每类最新安全演练具有
独立30天有效期，并拒绝超出5分钟时钟容差的未来记录；同一时效函数同时控制实时面板、readiness
闸门和评审包导入；计划、提交、确认、对账四个观测时间全部参与未来时间检查，当前提交不能掩盖未来确认
或未来对账，恰好5分钟为允许边界。因此重新导出不会让旧数据恢复新鲜。Worker 查询只装载最近30天观测，导入端则
逐条检查全部参与校准的记录；一条新观测不能掩盖同批过期样本。
观测状态还必须与成交和对账时间一致：`reconciled` 与对账时间成对出现，成交类状态必须有正成交数量和
成交均价，拒单、超时及未知状态必须为零成交和零手续费；导入文件不能只改状态标签来伪造成交或恢复证据。
Testnet 观测固化策略版本与成本模型修订组成的 cohort 指纹；Worker 只读取当前 cohort，导入证据出现
混合或不匹配版本时阻断。四类安全演练也绑定相同指纹，旧版本演练不得满足新 cohort；迁移前记录标记
为 `legacy`，不得静默并入当前成本建议或安全控制证明。
readiness 的六项晋级门槛固定为代码政策（样本外100笔/90天/最大10%回撤、Paper 100笔/最大
0.5个百分点收益退化、Testnet 100笔有效成交、开仓/平仓各至少40笔且至少40个相同 `tradeId` 的完整
往返交易），导入文件不能放宽；纯单侧成交或互不关联的两组开/平命令不能证明完整交易生命周期。Worker
将数据库已有 `trade_id` 带入原始证据，缺失关联的导入观测直接拒绝；平仓提交还必须发生在同一交易的
开仓确认之后，倒序配对不计入往返并报告无效交易 ID。开仓和平仓成交数量按交易汇总后还必须在百万分之一
相对误差或 `1e-8` BTC 数值容差内闭合，实质残仓与超额平仓均阻断并报告交易 ID。Testnet 的拒单、零成交、超时或
未知命令只计入执行观测，不得填充有效成交样本；累计成交数量还必须达到累计计划数量的95%，避免用极小
部分成交刷满样本。按全部命令观测计算的拒绝率固定不得超过5%，独立于按数量计算的成交率，避免大量成交
数量掩盖频繁的小额拒单。计划到实际提交的 P95 延迟固定不超过2000毫秒，不能用提交后的快速响应掩盖
陈旧决策；每笔有效成交还必须具备确认时间，提交到确认的 P95 延迟同样固定不超过2000毫秒，并且只按
成交样本计算，拒单不能稀释确认延迟。已对账的超时、未知或恢复命令仍计入异常恢复依赖，按全部观测计算的
依赖率固定不得超过5%；恢复成功率不能替代正常命令确认质量。同一幂等键必须只有一条最终观测；重复键
无论参数是否一致都阻断校准，不能复制同一命令来膨胀成交样本或稀释按命令统计的异常比例。
Paper 晋级材料同样固定100笔、0.5个百分点收益退化、5个百分点胜率退化、3个百分点回撤增加和2分钟
周期容差；独立 readiness
导出也会重算报告，拒绝调用方传入的不一致结论。进入 readiness、云端证据和综合评审包的回测成本
政策固定为至少0.05%单边手续费和0.05%单边滑点；构建与导入均重算回测，低于下限的成本模型只能
留在独立研究场景。若评审包同时包含 Testnet 校准，回测滑点还必须不低于其 P95 建议值；即使重算回测、
readiness 和外层摘要也不能绕过该跨证据约束。Testnet 平仓对账还会把开仓、平仓的 USDT 佣金原子回填
至对应执行观测；正值佣金换算出的 P95 单边费率同样成为回测手续费下限，未回填零值不能降低静态政策。
有效成交中至少80%必须具备正值手续费证据，避免少量已回填佣金掩盖大批占位零值。
版本化建议成本模型同时输出 P95 单边手续费、P95 滑点及各自样本数；无论评审接收原始证据包还是兼容的
校准报告对象，回测手续费都不得低于该建议值。版本号还包含排序后成交观测内容的稳定指纹；相同笔数但
内容不同的证据批次不会再共用成本模型版本，单纯调整数组顺序也不会制造新版本。
Paper 账本转换为偏差观测时会保留实际采用的单边手续费与滑点；当综合评审同时包含 Testnet 校准时，
每笔 Paper 观测都必须明确携带成本且不低于 Testnet 建议，旧格式缺失字段不能绕过跨证据约束。
手动 Paper 仓位预演同样计入往返滑点，并将其纳入盈亏平衡、止盈止损净值和风险预算；可用于评审的
手续费与滑点均执行至少0.05%的单边下限，成本版本明确编码实际采用的两项费率。
对应交易回归测试共150项通过。

| 阶段                             | 状态   | 当前证据                                                                                                                                               |
| -------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Phase 1 · 分钟交易路径与成本模型 | 完成 | `evaluateContractTradePath` 已覆盖多空、同柱冲突、跳空止损、时间退出、费用、滑点、资金费、最大不利波动、未结算状态和输入校验。 |
| Phase 2 · 分钟策略时序回测 | 完成 | 已连接历史决策帧与 Phase 1 结算，提供训练/验证/最终留出分段、市场状态分层、买入持有/趋势/确定性随机基线、绩效指标和可审计导出。 |
| Phase 3 · Paper偏差监控 | 完成 | Paper账本已持久化策略、信号、路径、行情源和成本版本；提供周/月版本隔离偏差队列及缺失链路、漏跑、数据断层和策略切换审计事件。 |
| Phase 4 · Testnet成交校准 | 完成 | Testnet执行已记录成交、拒单、超时、未知和恢复状态；提供滑点/延迟/成交率/幂等冲突校准、版本化成本建议、安全演练存档和API。 |
| Phase 5 · 受限实盘准备评审 | 完成（仅人工评审） | 已提供可导出的分层证据闸门、账户/风险/资格检查和固定禁令；代码硬性保持 `liveTradingAuthorized: false`，当前页面证据等级为“研究”。 |

## 2. 产品原则

- 研究结论、交易计划和执行结果是三个不同领域，不互相冒充。
- 信号必须同时展示支持证据、反对证据、数据时间和失效条件。
- 所有绩效默认扣除手续费、滑点和不利资金费。
- 回测、Paper、Testnet 和实盘样本必须分组统计。
- 策略版本、参数、行情口径和成本模型必须可以还原。
- 样本不足、数据过期或结果不稳定时，系统应拒绝升级而不是填充乐观估计。
- Paper/Testnet 是当前执行上限；不得复用浏览器变量保存交易密钥。

## 3. 目标链路

```text
市场数据
→ 数据质量与时效检查
→ 市场状态识别
→ 多证据信号
→ 交易计划
→ 账户风险闸门
→ 历史路径回测
→ Paper 观察
→ Testnet 成交校准
→ 策略晋级评审
```

任何上游环节失败，下游都必须降级为“不可执行”。

## 4. 分阶段开发目标

### Phase 1：分钟交易路径与成本模型

目标：让分钟合约的止损、止盈和持仓结果可以用同一套保守规则重复计算。

交付物：

- 纯函数交易路径评估模块。
- 支持多头、空头、止损、止盈、最长持仓和无结果状态。
- 同一分钟同时触发止损和止盈时按止损优先。
- 跳空越过止损时按更差的开盘价退出。
- 结果计入往返手续费、滑点和有方向的资金费。
- 返回退出原因、退出时间、毛损益、净损益、收益率和最大不利波动。
- 用公开 interface 覆盖关键成交语义的自动化测试。

验收：

- 所有 worked examples 的结果可人工复算。
- 多空规则镜像一致。
- 不读取当前时间、网络、浏览器或数据库。
- `npm run test:trading`、`npm run type-check` 和 `npm run build` 通过。

### Phase 2：分钟策略时序回测

目标：验证分钟合约决策，而不是只展示当前信号。

交付物：

- 严格按时间推进的信号生成，禁止未来数据进入当前决策。
- 训练、验证和最终留出区间。
- 使用 Phase 1 的路径与成本模型结算每笔计划。
- 按趋势、震荡、高波动状态拆分结果。
- 与持有、简单趋势和随机方向基准比较。
- 输出交易数、覆盖率、费用后期望、Profit Factor、最大回撤和连续亏损。

验收：

- 回测输入、策略版本、成本模型和结果可以导出。
- 留出区间不参与参数选择。
- 样本不足时不输出“有效”结论。

### Phase 3：Paper 与回测偏差监控

目标：发现离线结果与在线观察之间的漂移。

交付物：

- Paper 交易保存信号版本、计划价格、行情源和成本假设。
- 对比计划成交、Paper 成交和同期市场路径。
- 按周/月展示回测与 Paper 的期望差、胜率差和回撤差。
- 数据缺口、漏跑周期和策略切换形成审计事件。

验收：

- 不同策略版本不混算。
- 能解释任一 Paper 结果来自哪条信号与哪组参数。

### Phase 4：Testnet 成交校准

目标：用交易所测试环境校准订单层摩擦与故障处理。

交付物：

- 计划价格、提交时间、成交价格、成交时间和成交数量的偏差统计。
- 部分成交、重复请求、超时、订单未知和对账恢复场景。
- 滑点与费用模型根据 Testnet 观察更新，但保留版本历史。
- 紧急平仓、禁止新开仓和行情过期熔断的演练记录。

验收：

- 订单命令具备幂等标识。
- Worker 重启或请求超时后可以从交易所状态恢复。
- Testnet 结果不与 Paper 或回测样本混算。

### Phase 5：受限实盘准备评审

此阶段只形成评审材料，不默认增加主网适配器。

必须同时满足：

- 足够长的样本外正期望与可接受回撤。
- Paper 与 Testnet 表现没有无法解释的显著退化。
- 独立子账户、禁止提现、IP 白名单和最小权限密钥方案。
- 单笔、单日、单方向和组合级风险上限。
- 人工总开关、异常熔断、幂等下单和持续对账。
- 用户所在地、账户和产品资格已经单独确认。

## 5. 首个深模块

### Seam

建议在 `src/utils/contract-trade-path.ts` 暴露一个纯函数：

```ts
evaluateContractTradePath(input): ContractTradePathResult
```

调用方只需要提供：

- 方向、入场价格、止损、止盈和名义仓位。
- 入场后的完整 OHLC 分钟序列。
- 最大持仓分钟数。
- 单边手续费率、单边滑点率、资金费率和结算次数。

模块负责隐藏：

- 多空价格方向换算。
- 止损/止盈触发顺序。
- 同柱冲突和跳空规则。
- 时间退出。
- 毛损益、费用、资金费、净损益和最大不利波动计算。

### Interface invariants

- 只处理入场完成后的完整 K 线；调用方不得传入尚未收盘的 K 线。
- K 线必须按时间升序，价格必须为有限正数。
- 止损必须位于不利方向，止盈必须位于有利方向。
- 没有任何可用 K 线时返回未结算结果，不虚构退出价。
- 正资金费率表示多头支付、空头收取；风险计算不得用潜在资金费收益放大仓位。

### Error modes

- 无效方向、价格、K 线顺序或止损止盈关系：抛出明确错误。
- 到期但最终 K 线无有效收盘价：返回未结算并附原因。

该 seam 经确认后，按“一条行为测试 → 最小实现”的垂直切片开始 TDD。

## 6. 当前完成定义

只有当以下证据全部存在，才可以称项目“可用于交易参考”：

- 分钟策略具有无前视偏差的样本外回测。
- 费用、滑点和资金费后的期望为正。
- 回测、Paper 和 Testnet 三组结果均可审计且差异可解释。
- 页面明确展示当前策略所处证据等级和禁止事项。
- 风险闸门在行情过期、样本退化和系统故障时默认拒绝新开仓。

在此之前，项目应继续标记为研究与模拟工具。
