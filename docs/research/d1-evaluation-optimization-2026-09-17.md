# D1 策略评估读取优化

已部署 Worker：`95dcdfa0-e39d-46ad-93a8-16a711b9cbb8`。

部署后公网 `/api/health` 从当前机器连接超时（HTTP 000），因此未确认公网接口可达性；Cloudflare 控制面部署及远程 D1 只读验证已成功。不能将此次超时归因为应用故障或额度耗尽。

三类历史评估改为只投影计算所需字段；新增方向信号的部分覆盖索引；小时分组使用 NOT MATERIALIZED 避免中间表重复扫描。保留每小时首条样本、最近 120 个样本、24 个验证样本、空值处理、费率及交易周期。未引入时间缓存或缩短历史范围。

线上 D1 同数据、同参数读取对比（实际 SQL 只读查询，结果逐字段相等）：

| 查询 | 原读取行数 | 优化后 | 降幅 |
| --- | ---: | ---: | ---: |
| consensusStudy | 15254 | 10406 | 31.8% |
| strategyComparison | 9179 | 5763 | 37.2% |
| scoreThresholdStudy | 6482 | 3733 | 42.4% |

参数：模型 btc-signal-model-v2；比较使用全部市场状态；成本 0.1，阈值研究为 60/70。非所有费率和市场状态的线上穷举。新增本地 SQLite 回归验证旧/新投影、索引前后结果一致，覆盖多小时、多模型、等待信号与未完成结果，检查实际覆盖索引计划。

212 项测试、lint、Worker TypeScript、Vue 类型检查与 Worker 打包通过。线上已直接执行幂等的 0036 索引迁移（未批量执行其他迁移），之后发布 Worker。未来迁移系统重复执行 CREATE INDEX IF NOT EXISTS 安全。

索引建立一次读取 7432 行、写入 2374 行，数据库从约 6.04 MB 增至 6.25 MB；后续相关信号写入会产生索引维护成本。日累计额度不会因优化立即归零，实际每日降幅仍取决于查询频率和其他数据库流量，不能据单次实验承诺永不超限。

后续调用优化：前端行情、量化、管理、技术配置与提醒接口合并并发相同 GET（按 URL、请求头含身份、credentials 隔离）；不缓存完成的结果，不重试写操作，写操作开始和完成均清除在途引用。交易面板自动刷新从 1 分钟调为 5 分钟，切回标签页遵守刷新间隔；行情保持 1 分钟并避免重复在途请求。后台交易 Cron、权限校验和审计逻辑不变。管理 GET 不再无条件携带 JSON Content-Type，并补充请求超时。

后端初始化检查改为 LIMIT 1；量化快照原子比较最新完整 payload，未变化不新增快照（清理查询仍保留）。新增请求隔离、失败恢复、自动刷新节流及快照去重测试。共 215 项测试、lint、前后端类型检查、生产构建和 Worker dry-run 通过。Worker 发布版本 a4b66990-f8e4-44bb-99aa-55875e88ec7a；前端仅本地构建，未发布 GitHub Pages。此次未执行额外数据库迁移。

## 2026-09-18：按索引取最近样本

最新滚动 24 小时 insights 中 consensusStudy 与 strategyComparison 合计读取 4,452,024 行，第一轮优化仍不足。第二轮取消全历史小时窗口排序，改为倒序索引扫描、按小时索引查找首条符合方向/市场状态条件的行，收集到 120 条已完成样本停止；首条结果为空时仍跳过该小时，不以稍晚信号替代。训练/验证集改为一次条件聚合。没有时间缓存、额外写入、新索引或交易频率变更；不足 120 个有效小时的稀疏历史仍可能遍历全部历史。

线上只读旧/新 SQL 对比（同批请求，model=btc-signal-model-v2、regime=NULL、cost=0.1）：strategyComparison 6148 → 2782 行，consensusStudy 11065 → 2893 行；所有输出字段相等，合计降低 67.0%。此前仅合并聚合分别为 5916、10713 行，收益不足，未发布该中间方案。此次数值是固定参数单次测量，不是全账户日用量降幅保证。

保留旧 SQL fixture，回归覆盖不同模型/市场状态/费用、空集、少于 24 条、超过 120 条、空结果及重复时间戳。215 项测试、Worker 类型检查、lint、diff check 和打包检查通过；已部署版本 4caa4056-f8bc-4133-a1c6-cb8b47d27975，无数据库迁移。尚未测量发布后的完整计费日用量。

## 后续：待回填记录部分索引

新增并在线直接应用幂等迁移 0037_btc_auto_unresolved_outcomes.sql：按 market_source、observed_at 索引仅包含与 updateSignalOutcomes 查询条件一致的未完成记录，保留所有历史数据和原 SQL。未批量执行其他迁移，无需重新发布 Worker。迁移建立一次读取 7984 行、写入 2639 行，数据库大小约 6.71 MB；后续记录进入或退出待处理集合会有索引维护写入成本。

固定截止时间 2026-09-18T07:00:00.000Z 的线上迁移前后对比：Binance 5255 → 1 行（结果均为空）；Coinbase 765 → 40 行（均返回 40 条）。两组完整结果的 SHA-256 分别前后一致。该查询的实测降幅不是全账户每日降幅。216 项测试、lint、diff check 通过；新增测试覆盖选取结果不变、索引命中、已完成记录退出索引和重新待回填记录进入索引。
