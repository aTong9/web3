# GitHub Star < 1000 清理

按用户要求，沿用 2026-09-04 15:28–15:30 UTC 的已认证 GitHub API 核验快照，移除当前目录中 371 个 Star 为 100–999 的条目。此前已移除 96 个 Star < 100 条目。本次不重新查询实时 Star；恰好 1000 及无法核验的链接保留。

灾害应急、环境水务两个分类因无剩余条目，一并移除空分类。其余资源与分类保持原有内容。

## 恢复

[完整原始快照](github-stars-audit-2026-09-04.json)保留原分类、标题、URL、图标、描述及 Star 数。筛选 `stars >= 100 && stars < 1000` 即本次删除的 371 项；按 `category` 恢复条目，必要时重建上述两个分类即可。

未取消用户账号 Star，未删除任何 GitHub 仓库。Star 是历史查询快照，不代表未来数值或项目质量判断。
